<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0">
    <script src="https://unpkg.com/htmx.org@1.9.12"></script>
    <title>Hello 4AHWII ZOO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
          crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
            crossorigin="anonymous"></script>
</head>

<body>
    <main class="container mt-2">
        <div class="row">
            <div class="col border border-4 rounded-4 border-warning me-2">

                <h2 class="text-center mt-2">Meine Zoos</h2>
                <ul class="mb-2 alert">
                    <% zoos.forEach(e=> { %>
                    <li data-id="<%= e.id %>"
                        class="btn btn-warning  my-1"
                        hx-get="/zoo/<%= e.id %>"
                        hx-target="#details">
                        <%= `Land: ${e.land} Stadt: ${e.stadt} Adresse: ${e.adresse} (${e.baujahr})`%>
                    </li>
                    <!-- <% if (e.baujahr < 1950) { %>
                            <p>Der Zoo ist eher alt</p>
                            <% } else { %>
                                <p>Der Zoo ist NEU</p>
                                <% } %>
                            -->
                    <% }) %>
                </ul>
            </div>
            <div class="col border border-4 rounded-4 border-warning">
                <h3 class="text-center mt-2">Details:</h3>
                <ul class="mb-2 alert"
                    id="details">No Zoo selected yet</ul>
            </div>
        </div>
    </main>
</body>

</html>